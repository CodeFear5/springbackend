# Application Name
#spring.application.name=complaintsystem

# Database Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/complaint_db
#spring.datasource.username=root
#spring.datasource.password=ise123

# JPA / Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.sql.init.mode=always

# Ensure data.sql is loaded
#spring.datasource.initialization-mode=always

# Server Port
#server.port=8080
## JWT Configuration
#app.jwt.secret=AR8jDhwZXjeW4c4tq+m6Y3AVYeAMO7Y70HfNkkDideaftijD7meU38taj4z3JA85
#app.jwt.expiration-in-ms=86400000





spring.application.name=complaintsystem

# Database (PostgreSQL on Render or local fallback)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/complaint_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:password}

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Port
server.port=${PORT:8080}

# JWT
app.jwt.secret=${JWT_SECRET:AR8jDhwZXjeW4c4tq+m6Y3AVYeAMO7Y70HfNkkDideaftijD7meU38taj4z3JA85}
app.jwt.expiration-in-ms=${JWT_EXPIRATION:86400000}
